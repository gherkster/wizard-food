{
  "name": "@wizard/extensions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter './*' --if-present build",
    "dev": "pnpm -r --filter './*' --if-present dev",
    "typecheck": "pnpm -r --filter './*' --if-present typecheck"
  }
}
